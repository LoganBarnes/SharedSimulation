language: c++

branches:
  only:
    - master

sudo: false
dist: trusty

addons:
  apt:
    packages:
      - g++-5
      - g++-6
      - xvfb
      - xorg-dev libglu1-mesa-dev
    sources: &sources
      - ubuntu-toolchain-r-test
      - kubuntu-backports

cache:
  directories:
    - ${TRAVIS_BUILD_DIR}/deps/llvm-3.8.0
    - ${TRAVIS_BUILD_DIR}/deps/llvm-3.9.0

matrix:
  include:

    # Xcode 7.3
    - os: osx
      env: BUILD_TYPE=Release SHARED=OFF TESTS=ON
      osx_image: xcode7.3

    # Xcode 8
    - os: osx
      env: BUILD_TYPE=Release SHARED=OFF TESTS=ON
      osx_image: xcode8

    # GCC 5
    - os: linux
      env: COMPILER=g++-5 BUILD_TYPE=Release SHARED=OFF TESTS=ON
      compiler: gcc

    # GCC 6
    - os: linux
      env: COMPILER=g++-6 BUILD_TYPE=Release SHARED=OFF TESTS=ON
      compiler: gcc

    # clang 3.9
    - os: linux
      env: LLVM_VERSION=3.8.0 BUILD_TYPE=Release SHARED=OFF TESTS=ON
      compiler: clang

    # clang 3.9
    - os: linux
      env: LLVM_VERSION=3.9.0 BUILD_TYPE=Release SHARED=OFF TESTS=ON
      compiler: clang

    # shared libs
    - os: linux
      env: LLVM_VERSION=default BUILD_TYPE=Release SHARED=ON TESTS=ON
      compiler: clang

    # shared libs
    - os: linux
      env: LLVM_VERSION=default BUILD_TYPE=Release SHARED=ON TESTS=ON
      compiler: clang

    # Shared libs
    - os: osx
      env: BUILD_TYPE=Release SHARED=ON TESTS=ON
      osx_image: xcode8

    - os: linux
      env: LLVM_VERSION=default BUILD_TYPE=Release SHARED=ON TESTS=ON
      compiler: clang

    # Debug
    - os: osx
      env: BUILD_TYPE=Debug SHARED=OFF TESTS=ON
      osx_image: xcode8

    - os: linux
      env: LLVM_VERSION=default BUILD_TYPE=Debug SHARED=OFF TESTS=ON
      compiler: clang

install:
  ############################################################################
  # All the dependencies are installed in ${TRAVIS_BUILD_DIR}/deps/
  ############################################################################
  - DEPS_DIR="${TRAVIS_BUILD_DIR}/deps"
  - mkdir -p ${DEPS_DIR} && cd ${DEPS_DIR}

  ############################################################################
  # Setup default versions and override compiler if needed
  ############################################################################
  - if [[ "${LLVM_VERSION}" == "default" ]]; then LLVM_VERSION=3.9.0; fi
  - if [[ "${COMPILER}" != "" ]]; then export CXX=${COMPILER}; fi


  ############################################################################
  # Install a recent CMake
  ############################################################################
  - |
    if [[ "${TRAVIS_OS_NAME}" == "linux" ]]; then
      CMAKE_URL="https://cmake.org/files/v3.7/cmake-3.7.2-Linux-x86_64.tar.gz"
      mkdir cmake && travis_retry wget --no-check-certificate --quiet -O - ${CMAKE_URL} | tar --strip-components=1 -xz -C cmake
      export PATH=${DEPS_DIR}/cmake/bin:${PATH}
    else
      brew upgrade cmake || brew install cmake
    fi
  - cmake --version


  ############################################################################
  # Install Clang, libc++ and libc++abi
  ############################################################################
  - |
    if [[ "${LLVM_VERSION}" != "" ]]; then
      LLVM_DIR=${DEPS_DIR}/llvm-${LLVM_VERSION}
      if [[ -z "$(ls -A ${LLVM_DIR})" ]]; then
        LLVM_URL="http://llvm.org/releases/${LLVM_VERSION}/llvm-${LLVM_VERSION}.src.tar.xz"
        LIBCXX_URL="http://llvm.org/releases/${LLVM_VERSION}/libcxx-${LLVM_VERSION}.src.tar.xz"
        LIBCXXABI_URL="http://llvm.org/releases/${LLVM_VERSION}/libcxxabi-${LLVM_VERSION}.src.tar.xz"
        CLANG_URL="http://llvm.org/releases/${LLVM_VERSION}/clang+llvm-${LLVM_VERSION}-x86_64-linux-gnu-ubuntu-14.04.tar.xz"
        mkdir -p ${LLVM_DIR} ${LLVM_DIR}/build ${LLVM_DIR}/projects/libcxx ${LLVM_DIR}/projects/libcxxabi ${LLVM_DIR}/clang
        travis_retry wget --quiet -O - ${LLVM_URL}      | tar --strip-components=1 -xJ -C ${LLVM_DIR}
        travis_retry wget --quiet -O - ${LIBCXX_URL}    | tar --strip-components=1 -xJ -C ${LLVM_DIR}/projects/libcxx
        travis_retry wget --quiet -O - ${LIBCXXABI_URL} | tar --strip-components=1 -xJ -C ${LLVM_DIR}/projects/libcxxabi
        travis_retry wget --quiet -O - ${CLANG_URL}     | tar --strip-components=1 -xJ -C ${LLVM_DIR}/clang
        (cd ${LLVM_DIR}/build && cmake .. -DCMAKE_INSTALL_PREFIX=${LLVM_DIR}/install -DCMAKE_CXX_COMPILER=clang++)
        (cd ${LLVM_DIR}/build/projects/libcxx && make install -j2)
        (cd ${LLVM_DIR}/build/projects/libcxxabi && make install -j2)
      fi
      export CXXFLAGS="-nostdinc++ -isystem ${LLVM_DIR}/install/include/c++/v1"
      export LDFLAGS="-L ${LLVM_DIR}/install/lib -l c++ -l c++abi"
      export LD_LIBRARY_PATH="${LD_LIBRARY_PATH}:${LLVM_DIR}/install/lib"
      export PATH="${LLVM_DIR}/clang/bin:${PATH}"
    fi
  - ${CXX} --version


  ############################################################################
  # Install a CUDA
  ############################################################################
  # - |
  #   if [[ "${TRAVIS_OS_NAME}" == "linux" ]]; then source .travis/install-cuda-trusty.sh; fi

before_script:
  - cd ${TRAVIS_BUILD_DIR}

script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DBUILD_SHARED_LIBS=$SHARED -DBUILD_SHARED_TESTS=$TESTS -DUSE_GLFW=ON -DUSE_GLM=ON -DUSE_GUI=ON -DSTRICT_FLAGS=ON ..
  - cmake --build .

notifications:
  on_success: never
  on_failure: always